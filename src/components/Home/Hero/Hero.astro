---
import "./hero.scss";
import { Image } from "astro:assets";

const { image, title } = Astro.props.data;

const heroImageSrc = image?.node?.sourceUrl;
const heroAlt = image?.node?.altText || "";

import heroMountain from "../../../assets/hero-path.png";
---

<div class="home-hero">
  <div class="home-hero-background">
    <Image
      src={heroImageSrc}
      alt={heroAlt}
      format="webp"
      widths={[800, 1200, 1600, 2000]}
      sizes="100vw"
      loading="eager"
      inferSize={true}
      class="home-hero-background-img"
    />
  </div>
  <div class="home-hero-graphic">
    <img src={heroMountain.src} alt={"Green path"} />
  </div>
  <div class="home-hero-title">
    <h1 set:html={title} />
  </div>
  <div class="home-hero-color"></div>
  <div class="home-hero-fade"></div>
</div>

<script>
  // @ts-nocheck
  import { gsap } from "gsap";
  const image = document.querySelectorAll(".hero-background img");
  const tl = gsap.timeline();
  tl.add("start").from(
    image,
    { filter: "blur(100px)", duration: 2.2 },
    "start",
  );
</script>
